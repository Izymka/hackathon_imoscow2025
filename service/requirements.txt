# Runtime dependencies for the FastAPI service and ML inference
# Keep minimal but complete to run `service/main.py` and its called scripts.

# Web API
fastapi>=0.115.13
uvicorn>=0.29
pydantic>=2.5

# ML core
torch>=2.5.1
numpy>=1.24
omegaconf>=2.3
python-dotenv>=1.0

# Data handling / DICOM
pandas>=2.0
pydicom>=2.4
SimpleITK>=2.3

# Utilities
tqdm>=4.66

# Inference module imports Lightning module at import time
# (even though training is not used in the service), so we include its deps
pytorch-lightning>=2.2
torchmetrics>=1.2
#scikit-learn>=1.3
#matplotlib>=3.8
seaborn>=0.13
captum==0.8.0